<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="eazyonrent.Pages.ItemDetails"
             Title="Item Details"
             BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark=#1C1C1E}">
    <Grid RowDefinitions="*,Auto">

        <ScrollView>
            <VerticalStackLayout Spacing="20" Padding="16">

                <!-- Image Gallery Section -->
                <Frame BackgroundColor="{AppThemeBinding Light=White, Dark=#2C2C2E}" CornerRadius="15" HasShadow="True" Padding="16">
                    <VerticalStackLayout Spacing="15">
                        <!-- Main Image Display -->
                        <Frame CornerRadius="10" HasShadow="False" Padding="0" HeightRequest="300">
                            <Grid>
                                <!--<Image x:Name="MainImage" 
                                   Source="{Binding CurrentImage}"
                                   Aspect="AspectFill"
                                   BackgroundColor="#F0F0F0"/>-->

                                <!--<ScrollView x:Name="ImageScrollView"
                Orientation="Both">

                                    <Image x:Name="MainImage"
               Source="{Binding CurrentImage}"
               Aspect="AspectFit"
                                           HorizontalOptions="Fill"
               VerticalOptions="Fill"
               BackgroundColor="#F0F0F0"/>-->
                                <Image x:Name="MainImage"
                                       Source="{Binding CurrentImage}"
                                       Aspect="AspectFit"
                                       HorizontalOptions="Fill"
                                       VerticalOptions="Fill"
                                       BackgroundColor="#F0F0F0">

                                    <Image.GestureRecognizers>
                                        <PinchGestureRecognizer PinchUpdated="OnPinchUpdated"/>
                                        <PanGestureRecognizer PanUpdated="OnPanUpdated"/>
                                    </Image.GestureRecognizers>

                                </Image>


                                <!--</ScrollView>-->
                                <!-- Navigation Buttons -->
                                <Button x:Name="PrevButton" 
                                    Text="❮" 
                                    BackgroundColor="{AppThemeBinding Light=White, Dark=#3A3A3C}" 
                                    TextColor="{AppThemeBinding Light=Black, Dark=White}"
                                    WidthRequest="40" 
                                    HeightRequest="40"
                                    CornerRadius="20"
                                    HorizontalOptions="Start" 
                                    VerticalOptions="Center"
                                    Margin="10,0,0,0"
                                    Clicked="OnPrevImageClicked"/>

                                <Button x:Name="NextButton" 
                                    Text="❯" 
                                    BackgroundColor="{AppThemeBinding Light=White, Dark=#3A3A3C}" 
                                    TextColor="{AppThemeBinding Light=Black, Dark=White}"
                                    WidthRequest="40" 
                                    HeightRequest="40"
                                    CornerRadius="20"
                                    HorizontalOptions="End" 
                                    VerticalOptions="Center"
                                    Margin="0,0,10,0"
                                    Clicked="OnNextImageClicked"/>

                                <!-- Zoom Controls -->
                                <VerticalStackLayout HorizontalOptions="End" 
                                               VerticalOptions="End" 
                                               Spacing="5" 
                                               Margin="10">
                                    <Button Text="🔍+" 
                                        BackgroundColor="{AppThemeBinding Light=White, Dark=#3A3A3C}" 
                                        TextColor="{AppThemeBinding Light=Black, Dark=White}"
                                        WidthRequest="40" 
                                        HeightRequest="40"
                                        CornerRadius="20"
                                        Clicked="OnZoomInClicked"/> 
                                    <Button Text="🔍-" 
                                        BackgroundColor="{AppThemeBinding Light=White, Dark=#3A3A3C}" 
                                        TextColor="{AppThemeBinding Light=Black, Dark=White}"
                                        WidthRequest="40" 
                                        HeightRequest="40"
                                        CornerRadius="20"
                                        Clicked="OnZoomOutClicked"/>
                                </VerticalStackLayout>

                               


                                <!-- Image Counter -->
                                <Label x:Name="ImageCounter"
                                   Text="{Binding ImageCounterText}"
                                   BackgroundColor="Black" 
                                   TextColor="White"
                                   Padding="8,4"
                                   HorizontalOptions="Start" 
                                   VerticalOptions="End"
                                   Margin="10"
                                   FontSize="12"/>
                            </Grid>
                        </Frame>

                        <!-- Thumbnail Strip -->
                        <CollectionView x:Name="ThumbnailCollection" 
                                    ItemsSource="{Binding ItemImages}"
                                    ItemsLayout="HorizontalList"
                                    HeightRequest="80"
                                    SelectionMode="Single"
                                    SelectionChanged="OnThumbnailSelectionChanged">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="5">
                                        <Frame CornerRadius="8" HasShadow="False" Padding="0">
                                            <Image Source="{Binding .}" 
                                               WidthRequest="70" 
                                               HeightRequest="70"
                                               Aspect="AspectFill"/>
                                        </Frame>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                <!-- Category Selection -->
                <!--
                <Frame BackgroundColor="White" CornerRadius="15" HasShadow="True" Padding="16">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Category" FontSize="16" FontAttributes="Bold" TextColor="#333"/>
                        <Picker x:Name="CategoryPicker" 
                            ItemsSource="{Binding Categories}"
                            ItemDisplayBinding="{Binding CategoriesName}"
                            SelectedItem="{Binding SelectedCategory}"
                            BackgroundColor="#F8F9FA"
                            TextColor="#333"/>
                    </VerticalStackLayout>
                </Frame>-->

                <!-- Item Information -->
                <Frame BackgroundColor="{AppThemeBinding Light=White, Dark=#2C2C2E}" CornerRadius="15" HasShadow="True" Padding="16">
                    <VerticalStackLayout Spacing="15">
                        <VerticalStackLayout Spacing="5">
                            <Button x:Name="EditButton" 
        Text="Edit Item"
        IsVisible="False"
        Clicked="EditButton_Clicked"                            
                                    />

                            <Label Text="Category Name:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                            <Label Text="{Binding ItemData.CategoryName}" FontSize="18" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#333, Dark=White}"/>
                        </VerticalStackLayout>

                        <!-- Company Name -->
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Company Name:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                            <Label Text="{Binding ItemData.CompanyName}" FontSize="18" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#333, Dark=White}"/>
                        </VerticalStackLayout>

                        <!-- Item Name -->
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Item Name:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                            <Label Text="{Binding ItemData.ItemName}" FontSize="22" FontAttributes="Bold" TextColor="#1E40AF"/>
                        </VerticalStackLayout>

                        <!-- Description -->
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Description:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                            <Label Text="{Binding ItemData.ItemDescriptions}" FontSize="16" TextColor="{AppThemeBinding Light=#333, Dark=White}" LineBreakMode="WordWrap"/>
                        </VerticalStackLayout>

                        <!-- Cost -->
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Cost:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                            <Label Text="{Binding FormattedCost}" FontSize="28" FontAttributes="Bold" TextColor="#059669"/>
                        </VerticalStackLayout>

                        <!-- Available From -->
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Available From:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                            <Label Text="{Binding FormattedAvailableDate}" FontSize="16" TextColor="{AppThemeBinding Light=#333, Dark=White}"/>
                        </VerticalStackLayout>

                        <!-- Location -->
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Location:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                            <Label Text="{Binding ItemData.Location}" FontSize="16" TextColor="{AppThemeBinding Light=#333, Dark=White}"/>
                        </VerticalStackLayout>

                        <!-- Booking Status -->
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Status:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                            <Frame BackgroundColor="#DCFCE7" CornerRadius="20" HasShadow="False" Padding="12,6">
                                <Label Text="Available" TextColor="#166534" FontAttributes="Bold" HorizontalOptions="Center"/>
                            </Frame>
                        </VerticalStackLayout>

                        <!-- Rating and Views -->
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="20">
                            <VerticalStackLayout Grid.Column="0" Spacing="5">
                                <Label Text="Rating:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                                <HorizontalStackLayout Spacing="5">
                                    <Label Text="⭐⭐⭐⭐⭐" FontSize="18"/>
                                    <Label Text="Very Good" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}" VerticalOptions="Center"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1" Spacing="5">
                                <Label Text="Views:" FontSize="14" TextColor="{AppThemeBinding Light=#666, Dark=#AAAAAA}"/>
                                <Label Text="{Binding ViewCount}" FontSize="16" TextColor="{AppThemeBinding Light=#333, Dark=White}"/>
                            </VerticalStackLayout>
                        </Grid>

                    </VerticalStackLayout>
                </Frame>

                <!-- Similar Items Section -->
                <Frame BackgroundColor="{AppThemeBinding Light=White, Dark=#2C2C2E}" CornerRadius="15" HasShadow="True" Padding="16">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="Similar Items" FontSize="20" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#333, Dark=White}"/>

                        <CollectionView x:Name="SimilarItemsCollection" 
                                    ItemsSource="{Binding SimilarItems}"
                                    ItemsLayout="HorizontalList"
                                    HeightRequest="220">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="10" WidthRequest="180">
                                        <Frame BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark=#3A3A3C}" CornerRadius="10" HasShadow="True" Padding="12">
                                            <VerticalStackLayout Spacing="10">
                                                <Image Source="{Binding Images[0]}" 
                                                   WidthRequest="140" 
                                                   HeightRequest="100"
                                                   Aspect="AspectFill"/>
                                                <Label Text="{Binding ItemName}" 
                                                   FontSize="14" 
                                                   FontAttributes="Bold" 
                                                   TextColor="{AppThemeBinding Light=#333, Dark=White}"
                                                   LineBreakMode="TailTruncation"/>
                                                <Label Text="{Binding ItemCost, StringFormat='₹{0}'}" 
                                                   FontSize="16" 
                                                   FontAttributes="Bold" 
                                                   HorizontalTextAlignment="End"
                                                   TextColor="#059669"/>
                                            </VerticalStackLayout>
                                            <!-- Tap Gesture for Similar Items -->
                                            <Frame.GestureRecognizers>
                                                <TapGestureRecognizer Tapped="OnSimilarItemTapped" 
                      CommandParameter="{Binding .}"/>
                                            </Frame.GestureRecognizers>
                                        </Frame>


                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                <!-- Action Buttons -->


            </VerticalStackLayout>
        </ScrollView>
        <Frame BackgroundColor="{AppThemeBinding Light=White, Dark=#2C2C2E}" HasShadow="True" Padding="16" Grid.Row="1">
            <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                <Button Grid.Column="0"
                  Text="💬 Chat"
                  BackgroundColor="#3B82F6"
                  TextColor="White"
                  FontSize="18"
                  FontAttributes="Bold"
                  HeightRequest="50"
                  CornerRadius="25"
                  Clicked="OnChatClicked"/>

                <Button Grid.Column="1"
                  Text="📅 Book Now"
                  BackgroundColor="#059669"
                  TextColor="White"
                  FontSize="18"
                  FontAttributes="Bold"
                  HeightRequest="50"
                  CornerRadius="25"
                  Clicked="OnBookNowClicked"/>
            </Grid>
        </Frame>
    </Grid>
</ContentPage>
